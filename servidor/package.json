{
  "name": "peli-vs-peli_back-end",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set PORT=8080 && node src/server",
    "start-unix": "PORT=8080 node src/server",
    "dev": "set PORT=8080 && nodemon src/server --watch src"
  },
  "author": "Maleriandro",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "mysql": "^2.16.0",
    "nodemon": "^1.18.10"
  }
}
